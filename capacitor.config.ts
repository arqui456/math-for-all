import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ufal-math-with-ia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
